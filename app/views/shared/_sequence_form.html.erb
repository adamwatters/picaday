<%= form_for(@sequence, html: { multipart: true }) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :description, placeholder: "Add a description for your new sequence..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>