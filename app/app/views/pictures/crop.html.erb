<h1>Crop</h1>
<div>
  <%= image_tag @picture.image_url, id:"cropbox" %>
</div>
<%= form_for(@picture) do |f| %>
  <% %w[x y w h].each do |attribute| %>
    <%= f.text_field "crop_#{attribute}" %>
  <% end %>
  <div>
    <%= f.submit "Crop" %>
  </div>
<% end %>